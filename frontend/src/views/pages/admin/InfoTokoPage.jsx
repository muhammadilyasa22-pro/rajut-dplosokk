import { useEffect, useState } from "react";

const defaultInfo = { nama: "Toko Pengrajut D-PLOSOKK", pemilik: "Bu Endang", alamat: "", telepon: "", deskripsi: "Toko Pengrajut D-PLOSOKK milik Bu Endang." };

export default function InfoTokoPage() { const [form, setForm] = useState(defaultInfo); const [saved, setSaved] = useState(false); useEffect(() => { const data = localStorage.getItem("dplosokk_store_info"); if (data) setForm(JSON.parse(data)); }, []); function submit(e) { e.preventDefault(); localStorage.setItem("dplosokk_store_info", JSON.stringify(form)); setSaved(true); setTimeout(() => setSaved(false), 1800); } return <div><div className="page-actions"><div><h2>Info Toko</h2><p>Pengaturan informasi yang ditampilkan di frontend.</p></div></div>{saved && <div className="alert success">Informasi toko tersimpan di browser.</div>}<section className="admin-panel form-panel"><form onSubmit={submit} className="form-grid"><label>Nama toko<input value={form.nama} onChange={(e) => setForm({ ...form, nama: e.target.value })} /></label><label>Pemilik<input value={form.pemilik} onChange={(e) => setForm({ ...form, pemilik: e.target.value })} /></label><label>Telepon<input value={form.telepon} onChange={(e) => setForm({ ...form, telepon: e.target.value })} /></label><label>Alamat<input value={form.alamat} onChange={(e) => setForm({ ...form, alamat: e.target.value })} /></label><label className="full">Deskripsi<textarea rows="5" value={form.deskripsi} onChange={(e) => setForm({ ...form, deskripsi: e.target.value })} /></label><div className="full"><button className="btn btn-primary">Simpan Info Toko</button></div></form></section></div>; }
